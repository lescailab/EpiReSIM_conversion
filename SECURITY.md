# Security policy

Report vulnerabilities privately through the repository hosting platform's
security-reporting feature. Do not include sensitive reference datasets,
sample identifiers, access tokens, or private filesystem paths in reports.

Only the latest released minor version receives security fixes during the
experimental phase. Container images and dependency locks should be rebuilt
regularly; a rebuild does not bypass scientific and compatibility tests.
